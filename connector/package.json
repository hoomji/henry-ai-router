{
  "name": "connector",
  "version": "0.0.0",
  "private": true,
  "description": "Customer-side connector — calls providers directly under gateway-pushed ranked lists",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "sample": "node dist/src/dev/sampleApp.js",
    "e2e-app": "node dist/src/dev/e2eApp.js",
    "stream-probe": "node dist/src/dev/streamProbe.js",
    "real-provider-probe": "node dist/src/dev/realProviderProbe.js",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
