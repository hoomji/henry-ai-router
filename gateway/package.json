{
  "name": "gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Provider risk management gateway — M1 tracer",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/server.js",
    "stub": "node dist/src/dev/stubUpstream.js",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
